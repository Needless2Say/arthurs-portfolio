import type { BlogPost } from "@/types/portfolio";

// TODO: Add blog posts here when ready to publish.
// Each post needs: slug, title, date (YYYY-MM-DD), excerpt, tags, readTime (minutes).
export const BLOG_POSTS: BlogPost[] = [];
